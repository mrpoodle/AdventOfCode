lines = [line.strip("\n") for line in open("day01.txt")]
out = 0
for l in lines:
	first = None
	last = None
	for x in l:
		if x.isdigit():
			last = x
			if first:
				continue
			first=x
	out += int(first+last)
print(out)